name: Build Linux (Debug + Release + Packages)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.0'
          channel: 'stable'

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev libstdc++-12-dev libayatana-appindicator3-dev flatpak flatpak-builder

      # ═══════════════════════════════════════════════════════════
      #  Caches
      # ═══════════════════════════════════════════════════════════

      - name: Cache pub dependencies
        uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: pubcache-${{ hashFiles('pubspec.lock') }}
          restore-keys: pubcache-

      - name: Create assets directories
        run: |
          mkdir -p assets/images
          mkdir -p assets/icons

      - name: Get dependencies
        run: flutter pub get

      # ── Build debug ──
      - name: Build Linux debug
        run: flutter build linux --debug

      # ── Build release ──
      - name: Build Linux release
        run: flutter build linux --release

      # ═══════════════════════════════════════════════════════════
      #  Package: .deb
      # ═══════════════════════════════════════════════════════════

      - name: Package as .deb
        run: |
          # Get version from pubspec.yaml
          VERSION=$(grep 'version:' pubspec.yaml | head -1 | awk '{print $2}' | cut -d'+' -f1)
          
          # Create .deb directory structure
          DEB_DIR="dist/deb/ngskg-plus_${VERSION}_amd64"
          mkdir -p "$DEB_DIR/DEBIAN"
          mkdir -p "$DEB_DIR/usr/bin"
          mkdir -p "$DEB_DIR/usr/share/applications"
          mkdir -p "$DEB_DIR/usr/share/icons/hicolor/256x256/apps"
          
          # Copy release build
          cp -r build/linux/x64/release/bundle/* "$DEB_DIR/usr/bin/"
          
          # Create control file
          cat > "$DEB_DIR/DEBIAN/control" << EOF
          Package: ngskg-plus
          Version: $VERSION
          Architecture: amd64
          Maintainer: mjiutang <mjiutang@example.com>
          Description: NGS-KG+ Music Player
           A Flutter music player based on KuGou third-party API.
           Features include login, search, playlist, lyrics, and more.
          Depends: libgtk-3-0, libnotify4, libayatana-appindicator3-1, libmpv2
          EOF
          
          # Create postinst script (MIME database + desktop database update)
          cat > "$DEB_DIR/DEBIAN/postinst" << EOF
          #!/bin/sh
          set -e
          if command -v update-mime-database >/dev/null 2>&1; then
            update-mime-database /usr/share/mime >/dev/null 2>&1 || true
          fi
          if command -v update-desktop-database >/dev/null 2>&1; then
            update-desktop-database >/dev/null 2>&1 || true
          fi
          EOF
          chmod 755 "$DEB_DIR/DEBIAN/postinst"
          
          # Create desktop file
          cat > "$DEB_DIR/usr/share/applications/ngskg-plus.desktop" << EOF
          [Desktop Entry]
          Name=NGS-KG+
          Exec=/usr/bin/ngskg_plus %u
          Icon=ngskg
          Type=Application
          Categories=Audio;Music;Player;
          MimeType=application/x-ngskg-playlist;x-scheme-handler/ngskg;
          EOF
          
          # Copy icon
          cp assets/images/icon.png "$DEB_DIR/usr/share/icons/hicolor/256x256/apps/ngskg.png"
          
          # Install MIME XML
          mkdir -p "$DEB_DIR/usr/share/mime/packages"
          cp linux/mime/ngskg-plus.xml "$DEB_DIR/usr/share/mime/packages/"
          
          # Build .deb
          dpkg-deb --build "$DEB_DIR" "dist/ngskg-plus_${VERSION}_amd64.deb"

      # ═══════════════════════════════════════════════════════════
      #  Package: .flatpak
      # ═══════════════════════════════════════════════════════════

      - name: Package as .flatpak
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | head -1 | awk '{print $2}' | cut -d'+' -f1)

          cat > dist/com.mjiutang.ngskg.yml << EOF
          app-id: com.mjiutang.ngskg
          runtime: org.gnome.Platform
          runtime-version: '47'
          sdk: org.gnome.Sdk
          command: ngskg_plus

          modules:
            - name: ngskg
              buildsystem: simple
              build-commands:
                - mkdir -p /app/bin
                - cp -r * /app/bin/
                - chmod +x /app/bin/ngskg_plus
              sources:
                - type: dir
                  path: ../build/linux/x64/release/bundle

          finish-args:
            - --share=network
            - --share=ipc
            - --socket=x11
            - --socket=wayland
            - --socket=pulseaudio
            - --device=dri
            - --talk-name=org.kde.StatusNotifierWatcher
            - --talk-name=org.freedesktop.Notifications

          # Register ngskg:// protocol handler
          rename-desktop-file: ngskg-plus.desktop
          desktop-file-name-prefix: NGS-KG+
          desktop-file-name-suffix: " (Flatpak)"
          EOF

          # Copy desktop file with MimeType for protocol handler
          mkdir -p dist/build/files/share/applications
          cat > dist/build/files/share/applications/ngskg-plus.desktop << EOF
          [Desktop Entry]
          Name=NGS-KG+
          Exec=ngskg_plus %u
          Icon=ngskg
          Type=Application
          Categories=Audio;Music;Player;
          MimeType=application/x-ngskg-playlist;x-scheme-handler/ngskg;
          EOF

          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          flatpak install -y --user flathub org.gnome.Platform//47 || true

          flatpak-builder --user --install-deps-from=flathub --force-clean --repo=dist/repo dist/build dist/com.mjiutang.ngskg.yml

          flatpak build-bundle dist/repo dist/ngskg-plus.flatpak com.mjiutang.ngskg

      # ═══════════════════════════════════════════════════════════
      #  Package: PKGBUILD (Arch Linux)
      # ═══════════════════════════════════════════════════════════

      - name: Generate PKGBUILD
        run: |
          # Get version from pubspec.yaml
          VERSION=$(grep 'version:' pubspec.yaml | head -1 | awk '{print $2}' | cut -d'+' -f1)
          
          # Create PKGBUILD
          cat > dist/PKGBUILD << EOF
          # Maintainer: mjiutang <mjiutang@example.com>
          pkgname=ngskg-plus
          pkgver=$VERSION
          pkgrel=1
          pkgdesc="NGS-KG+ Music Player (Flutter)"
          arch=('x86_64')
          url="https://github.com/Tangmjiu/NGS-KG"
          license=('MIT')
          depends=('gtk3' 'libayatana-appindicator' 'libnotify' 'mpv')
          makedepends=('flutter' 'cmake' 'ninja' 'clang')
          
          source=("\$url/archive/refs/tags/v\$pkgver.tar.gz")
          sha256sums=('SKIP')
          
          build() {
            cd "NGS-KG-\$pkgver"
            flutter build linux --release
          }
          
          package() {
            cd "NGS-KG-\$pkgver"
            mkdir -p "\$pkgdir/usr/bin"
            cp -r build/linux/x64/release/bundle/* "\$pkgdir/usr/bin/"
            
            mkdir -p "\$pkgdir/usr/share/applications"
            cat > "\$pkgdir/usr/share/applications/ngskg-plus.desktop" << DESKTOP
          [Desktop Entry]
          Name=NGS-KG+
          Exec=/usr/bin/ngskg_plus %u
          Icon=ngskg
          Type=Application
          Categories=Audio;Music;Player;
          MimeType=application/x-ngskg-playlist;x-scheme-handler/ngskg;
          DESKTOP
            
            mkdir -p "\$pkgdir/usr/share/icons/hicolor/256x256/apps"
            cp assets/images/icon.png "\$pkgdir/usr/share/icons/hicolor/256x256/apps/ngskg.png"
            
            # MIME XML
            mkdir -p "\$pkgdir/usr/share/mime/packages"
            cp linux/mime/ngskg-plus.xml "\$pkgdir/usr/share/mime/packages/"
          }
          EOF
          
          # Create tarball for AUR
          tar -czf dist/ngskg-plus-PKGBUILD.tar.gz -C dist PKGBUILD

      # ── Upload artifacts ──
      - name: Upload debug build
        uses: actions/upload-artifact@v4
        with:
          name: ngskg-linux-debug
          path: build/linux/x64/debug/bundle

      - name: Upload release build
        uses: actions/upload-artifact@v4
        with:
          name: ngskg-linux-release
          path: build/linux/x64/release/bundle

      - name: Upload .deb package
        uses: actions/upload-artifact@v4
        with:
          name: ngskg-linux-deb
          path: dist/*.deb

      - name: Upload .flatpak bundle
        uses: actions/upload-artifact@v4
        with:
          name: ngskg-linux-flatpak
          path: dist/*.flatpak

      - name: Upload PKGBUILD
        uses: actions/upload-artifact@v4
        with:
          name: ngskg-linux-pkgbuild
          path: dist/PKGBUILD
